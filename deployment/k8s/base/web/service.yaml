apiVersion: v1
kind: Service
metadata:
  name: autodoc-web
  namespace: autodoc
  labels:
    app.kubernetes.io/part-of: autodoc
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
