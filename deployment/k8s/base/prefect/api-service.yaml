apiVersion: v1
kind: Service
metadata:
  name: prefect-api
  namespace: autodoc
  labels:
    app.kubernetes.io/part-of: autodoc
    app.kubernetes.io/component: prefect-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: prefect-api
  ports:
    - port: 4200
      targetPort: 4200
      protocol: TCP
